;; -*- mode: lisp -*-
(load "macros.ps")

(defview *Tools-modular-view
    model 'app
    class-name "ToolsAreaView"
    render (lambda ()
             (let ((html (array
                          "Modular"
                          )))
             ((@ this $el html) html))))
