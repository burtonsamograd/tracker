;; -*- mode: lisp -*-
(load "macros.ps")

(defun hex (x len)
  (setf len (or len 2))
  (when (= (typeof x) 'number)
    (let ((s ((@ x to-string) 16)))
      (when (< s.length len) ; zero pad
        (dotimes (i (+ 1 (- len s.length)))
          (setf s (+ '0 s))))
      s)))

